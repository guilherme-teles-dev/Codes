number = input("Array numbers: ").split()
print(number)
#Array right running
for c in number:
    print(c)
    aux = c
    #Array left running
    leftrunning = number.index(c)
    while(leftrunning > -1):
        if
        leftrunning -= 1